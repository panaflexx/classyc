/* Test 049: String array with mixed content */
#include <stdio.h>

int main() {
    String mixed[3] = {"apple", "banana", "cherry"};
    printf("mixed array: %s, %s, %s\n", mixed[0], mixed[1], mixed[2]);
    return 0;
}