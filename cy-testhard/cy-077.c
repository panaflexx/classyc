/* Test 077: String with array of strings */
#include <stdio.h>

int main() {
    String strings[3] = {"apple", "banana", "cherry"};
    printf("string array: %s, %s, %s\n", strings[0], strings[1], strings[2]);
    return 0;
}