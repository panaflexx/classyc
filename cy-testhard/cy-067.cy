/* Test 067: String array with different types */
#include <stdio.h>

int main() {
    String animals[3] = {"cat", "dog", "fish"};
    printf("string array: %s, %s, %s\n", animals[0], animals[1], animals[2]);
    return 0;
}